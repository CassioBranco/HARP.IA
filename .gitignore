# dependencies
/node_modules
/.pnp
.pnp.js
.yarn/install-state.gz

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem
Thumbs.db

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# env
.env
.env*.local

# vercel
.vercel

# typescript
*.tsbuildinfo

# storybook
storybook-static/

# supabase local
supabase/.branches/
supabase/.temp/

# memoria viva do projeto (notas privadas — fonte do harness via junction)
/memoria/

# playwright
playwright-report/
test-results/
.next-dev.log
docs/_backups/

# código legado / mockups — NÃO deployar (render antigo de blog sem sanitize)
/app_backup/
/_mockups/

# graphify — grafo de conhecimento gerado (reconstruível, não versionar)
/graphify-out/

# log do build local (regerado a cada npm run build)
.next-build.log

# backups automáticos de config (o original é que é versionado)
*.graphify-bak
