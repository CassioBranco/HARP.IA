# ANCOREO — Fila de trabalho noturno (2026-07-02)

> Autorização: Cássio concedeu permissão completa pra rodar a noite toda (2026-07-02).
> Portões mantidos: NADA de commit/push/deploy/migration_apply — só arquivos locais.
> Migrations novas são ESCRITAS mas não aplicadas. Protocolo: a cada acordada, pegar o
> próximo item ⬜, trabalhar, marcar ✅ com resumo de 1 linha, atualizar este arquivo,
> reagendar. `tsc --noEmit` verde antes de marcar qualquer item como feito.
> Design: seguir docs/DESIGN-NUCLEO.md (v2 "Carta Náutica") — e o pedido novo do Cássio:
> **levemente mais minimalista, principalmente nas telas pós-landing.**

## Fila (em ordem)

- ✅ **N1 — Passe minimalista + tema claro/escuro no núcleo**
  Feito: script anti-flash no root layout + `components/ThemeToggle.tsx` (localStorage
  `aco_theme`, respeita prefers-color-scheme); variantes `.dark` pra landing ("carta
  náutica à noite" #0B1F35/tinta-giz), auth, legal e consent; toggle montado na navbar
  da landing, no auth e no legal; passe minimalista: `.btn-naval`/`.panel-naval`
  achatados, sombras duras da landing 5→4px. tsc ✓, testado no preview (toggle
  alterna e persiste nos dois sentidos).

- ✅ **N2 — Onboarding v2 minimal + DEBUG completo como usuário**
  Feito: onboarding.css reescrito token-based (claro/escuro grátis, Fraunces+mono,
  "ficha de bordo"); penas/pássaro/aura removidos do TSX; âncora na marca. Debug real:
  conta de teste criada, fluxo das 7 telas executado clicando → chegou em /templates,
  perfil salvo no banco ✓, 11 eventos de telemetria ✓. 2 bugs corrigidos: autocomplete
  de cidades (Nominatim→lista IBGE local com busca sem acento) e subdomínio
  harpia.site→ancoreo.com.br. Relatório: docs/PROJETO/BUGS-ONBOARDING.md. tsc ✓.
  🚫 Gate Cássio: wildcard *.ancoreo.com.br (Vercel + Registro.br) pros subdomínios.

- ✅ **N3 — Painel + editor v2 minimal**
  Feito: painel.css (~330 linhas) e editor.css (~200 linhas) reescritos token-based
  (claro/escuro automáticos, superfícies planas 1px, mono em rótulos, Fraunces em h1,
  aura desligada); ThemeToggle na sidebar do painel. Bug consertado: auto-referência
  `--surf:hsl(var(--surf))` + mojibake de encoding (script node com mapa cp1252
  reverso). Validado no preview: sidebar/cards sólidos nos 2 temas, zero erros de
  console. tsc ✓.

- ✅ **N4 — Galeria de templates melhor + templates mais arrojados**
  Parte 1: galeria /templates reescrita v2 (escolher-modelo.css token-based
  claro/escuro, papel quente, Fraunces + mono, aura/grain removidos, âncora na marca,
  ThemeToggle no topo); grade 2 colunas = prévia maior (16:10); filtro por segmento
  (8 chips, tags em model-data); botão "Ver prévia" em nova aba + evento
  template_preview na telemetria. Validado (10 cards, filtro Educação→3, Todos→10).
  Parte 2: SiteReveal novo (shared) = scroll-reveal estilo Framer nos 10 layouts
  (IntersectionObserver, seguro sem JS, respeita reduced-motion, init pós-load pra
  não conflitar com hidratação — bug de mismatch achado e corrigido); polish de
  micro-interações em Clean/Profissional/Conversão (zoom suave de imagem, lift de
  cards/botões, saturação no hover — padrões de Framer/Wix/Squarespace); Academia
  já era arrojado (marquee, sticker, marca-texto). Validado no preview (reveal
  dispara ao rolar, zero erro novo de console). tsc ✓.

- ✅ **N5 — Triangulação de hyperlinks entre artigos (internal linking)**
  Feito: lib/seo/triangulation.ts — núcleo puro (keywords sem stopwords PT-BR,
  Jaccard, clusters por componentes conexos, plano com triângulos A→B→C→A +
  par bidirecional + related até 2–4 saídas/artigo) e injeção no HTML (âncora
  natural inline no 1º parágrafo válido — pula headings e <a> existentes;
  fallback "Leia também"; idempotente por href). Integrado em /api/publish/blog:
  cada publish triangula o site todo = artigo novo + backfill dos antigos.
  Migration 20260702020000 (kind/context/rendered) ESCRITA, NÃO aplicada —
  código funciona sem ela. Validação: 13/13 checks num teste de fixtures
  (5 artigos, 2 clusters → triângulo no cluster "barba", 4↔5 bidirecional,
  inline + leia-também, 2ª passada não duplica). tsc ✓.
  🚫 Gate Cássio: aplicar a migration de manhã (mcp apply_migration).

- ✅ **N6 — Ferramentas de personalização do editor (benchmark mercado)**
  Auditoria: o editor JÁ tinha paleta (19 nomeadas + personalizada), troca de fonte,
  troca de modelo, imagens (upload + galeria), marca e preview mobile (390×720) —
  paridade com o essencial de Wix/Squarespace. Implementado o que faltava:
  **Desfazer** (pilha LIFO pra modelo/cor/fonte, padrão Framer/Webflow; arrastar o
  color picker não inunda o histórico) + limpeza de restos HARPIA no editor
  (ph-bird→âncora, harp-ia.com→ancoreo.com.br, overlay de geração roxo→navy/vermelho
  do núcleo) + bug de tema claro (#fff fixo na amostra de fonte → var(--ink)). tsc ✓.
  FORA DO ESCOPO noturno (anotado): reordenar/ocultar seções exige refactor dos 10
  layouts (ordem hardcoded no JSX) — proposta pro backlog.
  Nota: validação em runtime do editor pede um site criado (conta de teste não tem;
  criar dispara geração IA = custo). Fica pro smoke test com Cássio de manhã.

- ✅ **N7 — Ferramenta de blog melhor**
  Feito: **sugestão de links internos no PostEditor** (integra o núcleo do N5 —
  extractKeywords + Jaccard client-side; top 3 artigos publicados por afinidade,
  botão insere `[título](/blog/slug)` no cursor; sugestão some quando já linkado);
  **SEO score ampliado** (7 checks ao vivo: título ≤60, meta 80–160, 600+ palavras,
  H2, FAQ, link interno); cor hardcoded do tema escuro corrigida (var(--ink)).
  Migration 20260702030000 (cover_image + scheduled_at + índice de agendados)
  ESCRITA, NÃO aplicada — capa/agendamento seguem "em breve" na UI até aplicar
  (o job de publicação agendada é passo seguinte). /blog validado no preview,
  zero erro novo. tsc ✓.
  🚫 Gate Cássio: aplicar migration + decidir job de agendamento (cron Vercel?).

- ✅ **N8 — Agentes/skills internos do ANCOREO**
  Feito: 4 agentes novos em .claude/agents — **ancoreo-designer** (guardião do
  núcleo v2, proibições HARPIA, workflow com preview nos 2 temas),
  **ancoreo-qa** (roteiro de debug como usuário, conta de teste, relatório
  BUGS-*.md), **ancoreo-linker** (arquitetura da triangulação + fixture-test
  canônico), **ancoreo-blogger** (mapa do pipeline artigo→gate AEO→publish +
  pendências). README.md dos agents com tabela e regras da casa; designer.md
  antigo marcado [SUBSTITUÍDO] (histórico, sem deletar). Skill nova
  .claude/skills/design-nucleo (resumo operacional do v2 pro loop principal).

- ✅ **N9 — Passada final**
  `next build` de produção VERDE (exit 0, todas as rotas; shared JS 87.4 kB).
  RELATORIO-NOITE.md escrito: resumo N1–N9, 5 gates pra aprovação de manhã
  (2 migrations, deploy, wildcard, job de agendamento, dados da empresa),
  decisões tomadas e roteiro de conferência. FILA CONCLUÍDA. 🎉

## Log de progresso

- 2026-07-02 00:5x — Fila criada; permissões concedidas; loop noturno iniciado.
- 2026-07-02 01:0x — N1 ✅ (tema claro/escuro + passe minimalista). Próximo: N2 (onboarding v2 + debug).
- 2026-07-02 01:2x — N2 ✅ (onboarding v2 + debug completo; 2 bugs corrigidos, funil validado ponta a ponta). Próximo: N3 (painel + editor v2).
- 2026-07-02 01:4x — N3 ✅ (painel + editor v2 minimal; encoding + --surf consertados; validado nos 2 temas). Cássio reforçou autonomia total. Próximo: N4 (galeria + templates arrojados).
- 2026-07-02 02:0x — N4 parte 1 ✅ (galeria /templates v2: filtro por segmento, prévia maior, Ver prévia, ThemeToggle). Próximo: N4 parte 2 (os 4 templates de cliente arrojados).
- 2026-07-02 02:2x — N4 ✅ (SiteReveal scroll-reveal nos 10 layouts + micro-interações em Clean/Profissional/Conversão; bug de hidratação achado e corrigido). Próximo: N5 (triangulação de hyperlinks).
- 2026-07-02 02:4x — N5 ✅ (triangulação contextual: serviço puro testado 13/13, injeção no HTML, integração no publish c/ backfill; migration escrita SEM aplicar). Próximo: N6 (personalização do editor).
- 2026-07-02 03:0x — N6 ✅ (Desfazer no editor + limpeza HARPIA + bug tema claro; reordenar seções anotado como refactor futuro). Próximo: N7 (ferramenta de blog).
- 2026-07-02 03:2x — N7 ✅ (sugestões de link interno no PostEditor via núcleo N5, SEO score 7 checks, migration capa/agendamento escrita). Próximo: N8 (agentes/skills internos).
- 2026-07-02 03:4x — N8 ✅ (4 agentes + README + skill design-nucleo; designer antigo marcado substituído). Próximo: N9 (passada final: next build + RELATORIO-NOITE.md).
- 2026-07-02 04:xx — N9 ✅ (build de produção verde; RELATORIO-NOITE.md pronto). **FILA NOTURNA CONCLUÍDA — 9/9.** Loop encerrado.
