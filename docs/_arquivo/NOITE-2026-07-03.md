# ANCOREO — Fila de trabalho noturno (2026-07-03)

> Autorização: Cássio pediu implementação hoje à noite das features priorizadas
> a partir da análise Hostinger, usando o modelo **Fable 5** pra codar (cada
> item é implementado por um Agent com `model:"fable"`; esta sessão orquestra,
> valida com tsc/preview e mantém o log).
> Portões mantidos (mesmo padrão da noite 2026-07-02): NADA de commit/push/
> deploy/migration_apply — só arquivos locais. Migrations novas são ESCRITAS
> mas não aplicadas. `tsc --noEmit` verde antes de marcar qualquer item feito.
> Design: seguir docs/DESIGN-NUCLEO.md (v2 "Carta Náutica") — públicas
> (landing/legal) impresso naval, internas (painel/editor/galeria) flat minimal.
> Critério de priorização do Cássio: força de SEO + melhora de UX pro usuário
> da plataforma E pro cliente do usuário (visitante do site final).

## Fila (em ordem — da análise Hostinger re-ranqueada por SEO+UX dupla)

- ✅ **NN1 — Verificador de links quebrados (broken-link checker)**
  Feito (Agent fable): `lib/seo/link-checker.ts` (extração de links do HTML/
  JSONB, classificação interno/externo, checagem HEAD→fallback GET com
  timeout 5s e concorrência limitada a 5); `app/api/score/[siteId]/links/route.ts`
  (GET autenticado, RLS, monta rotas reais do site + links dos blog posts,
  retorna total/quebrados); card novo "Links quebrados" em
  `app/(dashboard)/metrics/MetricsView.tsx` (verificação sob demanda por
  botão, lista os 10 primeiros quebrados). Sem migration (roda sob demanda,
  não persiste). tsc ✓ (conferido nesta sessão também).

- ✅ **NN2 — Widget de FAQ/chat (WhatsApp ou IA) no site do cliente**
  Feito (Agent fable): `components/templates/shared/ChatWidget.tsx` (botão
  flutuante + acordeão de FAQ + CTA WhatsApp, neutro, sem tokens da
  plataforma) integrado nos 10 layouts via `LayoutRenderer.tsx` (mesmo ponto
  do SiteReveal); `SiteSocials.tsx` ajustado (prop `raise`) pra não sobrepor;
  edição manual de FAQ adicionada no editor (`SectionEditor.tsx`); WhatsApp
  já existia (`onboarding_profiles.social_links.whatsapp`) mas nunca tinha
  migration — escrita `20260702040000_social_links_onboarding.sql`
  (idempotente, NÃO aplicada). tsc ✓ (conferido nesta sessão também).

- ✅ **NN3 — Motor de agendamento/reserva (booking)**
  Feito (Agent fable, 2ª tentativa): `components/templates/shared/BookingWidget.tsx`
  (botão flutuante canto inferior esquerdo, form com data/hora nativos + serviço,
  neutro, não grava em modo preview) integrado via `LayoutRenderer.tsx` (10
  layouts); `app/api/booking/route.ts` (POST público, valida site `published`
  + `booking_enabled`, grava via admin client — sem policy de insert anon
  direta); `app/(dashboard)/agendamentos/` (lista de solicitações do dono,
  troca de status); toggle "Agenda" no editor (`CustomizationPanel.tsx`) salva
  `sites.booking_enabled`. Migration `20260702050000_booking_requests.sql`
  (tabela `booking_requests` + coluna `sites.booking_enabled`, RLS por tenant)
  ESCRITA, NÃO aplicada. Notificação por e-mail: sem infra (sem Resend
  instalado) — TODO deixado no código, dono acompanha por /agendamentos.
  ⚠️ Gate de ordem: migration precisa ser aplicada ANTES do deploy deste
  código (editor/build-site-content já selecionam `booking_enabled`). tsc ✓
  (conferido nesta sessão também).

- ✅ **NN4 — Formulário de captura de lead não-intrusivo**
  Feito (Agent fable): padrão escolhido foi faixa INLINE ("Fique por dentro")
  no fluxo normal da página (última seção do site) — zero overlay, zero risco
  de interstitial penalty, zero disputa de canto com ChatWidget/BookingWidget.
  `components/templates/shared/LeadCaptureBar.tsx` (nome + e-mail/telefone +
  interesse, X dispensa e lembra 1 dia via localStorage); `app/api/leads/route.ts`
  (POST público, mesmo padrão do booking: valida site publicado + `leads_enabled`,
  grava via admin client); `app/(dashboard)/leads/` (listagem espelhando
  /agendamentos, filtro por status); toggle "Leads" no editor
  (`CustomizationPanel.tsx`). Migration `20260702060000_leads.sql`
  (`sites.leads_enabled` + tabela `leads`, RLS por tenant, sem insert anônimo)
  ESCRITA, NÃO aplicada — rota só funciona de fato após aplicar. tsc ✓
  (conferido nesta sessão também).

- ✅ **NN5 — Geração de descrição/imagem de produto via IA**
  Feito (Agent fable): reaproveitado o provider já usado no onboarding
  (Anthropic via `lib/claude/client.ts`, `getAnthropicClient()` +
  `MODELS.generate` + `cachedSystem()` + `friendlyAIError()` — nenhum provider
  novo). `app/api/ai/generate-description/route.ts` (POST autenticado, valida
  ownership do site via RLS, contexto de `onboarding_profiles`, sanitiza texto,
  registra em `ia_generations`). `SectionEditor.tsx`: a seção `services` ganhou
  editor de itens (nome/descrição/ícone) com botão "Gerar com IA" (ícone
  Phosphor `ph-sparkle`) ao lado da descrição — desabilita durante geração,
  erro vira "Erro, tente de novo" sem quebrar o editor. Sem migration (persiste
  em `sections.content` já existente). tsc ✓ (conferido nesta sessão também).

- ✅ **NN6 — Estender o SEO score ao vivo do blog pro site principal**
  Feito (Agent fable): núcleo dos 7 checks extraído pra `lib/seo/score.ts`
  (`buildPostSeoChecks`/`buildPageSeoChecks`/`computeSeoScore`, compartilhado);
  `PostEditor.tsx` refatorado pra usar o módulo (comportamento idêntico, zero
  duplicação). Adaptações pra página: título = `pages.title`, meta =
  `pages.meta_description`, palavras = texto visível das sections (piso 300 em
  vez de 600), H2 = section não-hero com título, FAQ = section `faq` com
  itens, link interno = link relativo nas sections. Painel novo
  `SeoPanel.tsx`, aba "SEO" no `CustomizationPanel` (barra + checklist igual
  ao editor de posts, campos editáveis de título/meta com contador). Sem
  migration (usa colunas já existentes de `pages`). tsc ✓ (conferido nesta
  sessão também).

- ✅ **NN7 — Passada final**
  `npm run build` de produção VERDE (exit 0, todas as rotas novas presentes:
  /agendamentos, /leads, /api/booking, /api/leads, /api/ai/generate-description,
  /api/score/[siteId]/links; shared JS 87.4 kB). `RELATORIO-NOITE-2026-07-03.md`
  escrito: resumo NN1-NN6, gates pra aprovação de manhã (3 migrations novas —
  social_links, booking_requests, leads — + gates herdados de 2026-07-02 ainda
  pendentes, infra de e-mail ausente, deploy). FILA CONCLUÍDA. 🎉

## Log de progresso

- 2026-07-02 (tarde) — Fila criada a partir da análise Hostinger re-ranqueada
  por SEO+UX dupla. Próximo: NN1 (broken-link checker), via Agent(model:fable).
- 2026-07-02 (noite) — NN1 ✅ (verificador de links quebrados: núcleo +
  API + card no painel de métricas). tsc verde. Próximo: NN2 (widget de
  FAQ/WhatsApp), via Agent(model:fable).
- 2026-07-02 (noite) — NN2 ✅ (widget de chat/FAQ + WhatsApp nos 10 templates,
  edição de FAQ no editor, migration social_links escrita). tsc verde.
  Próximo: NN3 (motor de agendamento/reserva), via Agent(model:fable).
- 2026-07-02 (noite) — 1ª tentativa de NN3 caiu por erro de conexão da API
  (agent morreu com só 72 tokens/13 tool uses, "completed" mas na verdade
  incompleto). Conferido `git status --porcelain`: zero arquivos de booking
  criados, nada pra desfazer. `tsc --noEmit` seguia limpo. Relançado NN3 do
  zero via novo Agent(model:fable).
- 2026-07-02 (noite) — NN3 ✅ na 2ª tentativa (booking widget + API pública +
  painel de agendamentos + toggle no editor; migration escrita, NÃO aplicada;
  sem infra de e-mail, TODO documentado). Conferido git status + tsc
  pessoalmente (não só o self-report do agent). Próximo: NN4 (formulário de
  captura de lead não-intrusivo), via Agent(model:fable).
- 2026-07-02 (noite) — NN4 ✅ (faixa inline de captura de lead, sem overlay/
  popup; API + painel /leads + toggle no editor; migration leads escrita,
  NÃO aplicada). Conferido git status + tsc pessoalmente. Próximo: NN5
  (geração de descrição de produto via IA), via Agent(model:fable).
- 2026-07-02 (noite) — NN5 ✅ (botão "Gerar com IA" no editor de serviços,
  reaproveitando o provider Anthropic do onboarding; API autenticada, sem
  migration). Conferido git status + tsc pessoalmente. Próximo: NN6
  (estender SEO score ao vivo pro site principal), via Agent(model:fable).
- 2026-07-02 (noite) — NN6 ✅ (núcleo dos 7 checks extraído pra lib/seo/score.ts,
  reaproveitado no PostEditor; aba SEO nova no editor do site com os mesmos
  checks adaptados pra página; sem migration). Conferido git status + tsc
  pessoalmente. Próximo: NN7 (passada final: next build + relatório),
  feito diretamente nesta sessão.
- 2026-07-02 (noite) — NN7 ✅ (`npm run build` verde, RELATORIO-NOITE-2026-07-03.md
  escrito). **FILA NOTURNA CONCLUÍDA — 7/7.** Loop encerrado.
